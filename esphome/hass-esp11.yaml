esphome:
  name: hass-esp11
  platform: ESP32
  board: esp32dev

# Enable logging
logger:
  baud_rate: 0

switch:
  - platform: restart
    id: esp11_restart
    name: "esp11 Restart"

# Enable Home Assistant API
api:

ota:
  password: !secret ota_password

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password

sensor:

  - platform: wifi_signal
    name: "esp11 WiFi Signal"
    update_interval: 60s
    
  - platform: uptime
    name: "esp11 Uptime"
    update_interval: 60s